<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type loki
  endpoint_url "http://loki:3100/loki/api/v1/push"
  labels {"job":"fluentd", "host":"#{Socket.gethostname}"}
  tenant_id tenant1
</match>
